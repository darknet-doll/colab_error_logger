from setuptools import setup, find_packages

setup(
    name="colab_error_logger",
    version="0.1.0",
    description="Automatic error logging for Colab notebooks into SQLite.",
    author="Your Name",
    packages=find_packages(),
    python_requires=">=3.7",
)
